services:
  - type: web
    name: trading-journal-api
    env: node
    buildCommand: cd server && npm install
    startCommand: cd server && node index.js
    envVars:
      - key: JWT_SECRET
        value: TradingJournalSuperSecretKey2024XYZ
      - key: NODE_ENV
        value: production
